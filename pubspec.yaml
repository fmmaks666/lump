name: lump
description: A simple package manager for Luanti
version: 1.0.0
repository: https://github.com/fmmaks666/lump
topics:
    - luanti
    - packages
    - mods

environment:
  sdk: ^3.6.0

# Add regular dependencies here.
dependencies:
  http: ^1.4.0
  args: ^2.7.0
  archive: ^4.0.7
  toml: ^0.16.0
  logging: ^1.3.0
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0

executables:
    lump:
